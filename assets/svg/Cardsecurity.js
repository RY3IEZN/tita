/** @format */

import * as React from "react";
import Svg, { Circle, Rect, SvgUri } from "react-native-svg";
import SVGImg from "./cardsecurity.svg";

export default function Cardsecurity(props) {
  return <SVGImg width={40} height={40} />;
}
