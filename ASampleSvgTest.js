/** @format */

import * as React from "react";
import Svg, { Circle, Rect, SvgUri } from "react-native-svg";
import SVGImg from "./assets/svg/loginpage.svg";

export default function SvgComponent(props) {
  return <SVGImg width={400} height={400} />;
}
